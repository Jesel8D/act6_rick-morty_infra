# Variables de entorno de la infraestructura local (Docker Compose)
# Estas credenciales son usadas por el contenedor de PostgreSQL

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=rickandmorty
